# COCKPIT3-B-D Acceptance Pass v1

## Status

Not yet evaluated.

## Function

This file defines the acceptance grammar for **COCKPIT3-B-D / Secondary Family and Multi-Family Grammar Floor v1**.
It does not claim that secondary family emission is already warranted.
It records what must become true before any lawful secondary-family verdict can be issued.

---

## Primary verdict set

- `PASS_SECONDARY_FAMILY_GRAMMAR_FLOOR`
- `PASS_PRIMARY_LABEL_DISCIPLINE_RETAINED`
- `HOLD_SECONDARY_FAMILY_NOT_YET_WARRANTED`
- `FAIL_SECONDARY_FAMILY_GRAMMAR_NOT_FORMED`

---

## Required acceptance checks

### 1. Primary-label default check

The grammar must explicitly preserve one-primary-label-per-axis as the current default.

### 2. Secondary-label admissibility check

The grammar must explicitly define what would have to become true before a secondary label is lawful.

### 3. Non-redundancy check

The grammar must explicitly reject secondary labels that add no real governance information beyond the primary label plus prose.

### 4. Checker-clarity check

The grammar must preserve checker visibility and avoid turning the bounded prototype into a noisy multi-label surface.

### 5. Boundary-honesty continuity check

The grammar must preserve all current non-upgrade and anti-execution discipline.

---

## Pass condition

`PASS_SECONDARY_FAMILY_GRAMMAR_FLOOR` is lawful only if all five required checks pass.

`PASS_PRIMARY_LABEL_DISCIPLINE_RETAINED` is lawful only if the grammar clearly says that primary labels remain the default until a stronger object proves otherwise.
