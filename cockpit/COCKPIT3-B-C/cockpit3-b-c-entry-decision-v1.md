# COCKPIT3-B-C Entry Decision v1

## Status

Entered at current threshold.

## Function

This file records the entry decision for **COCKPIT3-B-C / Emitted Review Family Labels Lift v1**.

COCKPIT3-B-B already defined the minimum family grammar for `review_trigger` and `review_scope`.
The next lawful step is to connect those families back into the bounded COCKPIT3-B-A prototype as emitted labels and checker-visible surface fields.

---

## One-sentence entry verdict

Current entry verdict:

> **ENTER_COCKPIT3_B_C_EMITTED_REVIEW_FAMILY_LABELS_LIFT**
>
> The project may now open COCKPIT3-B-C because the review-family floor has already been defined, and the next lawful threshold is to emit compact review-family labels inside the bounded prototype and checker surface without widening authority or opening `operator_review_required` itself.

---

## Immediate purpose

COCKPIT3-B-C exists to do four things:

1. emit review-trigger family labels inside the prototype;
2. emit review-scope family labels inside the prototype;
3. attach those labels to the checker-visible surface;
4. preserve the current non-upgrade clause and anti-execution holds.

---

## Boundary

COCKPIT3-B-C does **not**:

- authorize execution
- authorize governed mutation
- authorize verdict resolution
- claim that any current surface is already lawfully classified as `operator_review_required`
- turn review families into execution classes

It only connects the review-family floor back into the bounded prototype surface.
