# COCKPIT1-C Prototype Calibration Pass v1

## Status

Drafted for evaluation.

## Function

This file records what the static cockpit prototype must be calibrated against before any stronger frontend claim becomes lawful.

---

## Calibration anchors

The prototype must stay aligned with:

- `COCKPIT1-B` case state fixtures
- `COCKPIT1-A` object view bindings
- `COCKPIT1-A` screen state mapping
- the real current stage of `h-pylori-ulcer`
- the real current route and public-layer ecology of `power-posing`

---

## Minimum calibration checks

### 1. Bound-case presence check
The landing state must visibly contain both bound cases.

### 2. H. pylori verdict honesty check
The verdict-boundary screen must visibly retain current non-upgrade boundaries and retained holds.

### 3. Power-posing dissent density check
The dissent-pressure screen must not collapse the case into a generic controversy card.
It must keep replication, withdrawal, and methodological pressure visible as distinct pressure lines.

### 4. Route complexity check
The power-posing route state must visibly encode more than one route or entry surface.

### 5. Sidecar subordination check
The sidecar must remain obviously secondary to the main object panel.

---

## Current verdict grammar

- `PASS_STATIC_PROTOTYPE_SEMANTIC_FLOOR`
- `HOLD_STATIC_PROTOTYPE_NEEDS_RECALIBRATION`
- `FAIL_STATIC_PROTOTYPE_DRIFT`

`PASS_STATIC_PROTOTYPE_SEMANTIC_FLOOR` is lawful only if all five minimum calibration checks pass.

---

## Final calibration block

- calibration checks: 5
- semantic pass requirement: all checks pass
- stronger implementation claim before pass: forbidden
