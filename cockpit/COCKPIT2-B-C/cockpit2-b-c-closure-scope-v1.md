# COCKPIT2-B-C Closure Scope v1

## Status

Drafted for current threshold.

## Function

This file defines the closure scope for COCKPIT2-B-C.
It exists to make the final structured-card closure precise and honest.

---

## Scope rule

COCKPIT2-B-C should not widen the trigger universe beyond the current bounded set.
It should only close the one remaining gap inside the already declared trigger set.

---

## Current closure target

The current closure target is:

- `suggest canonical continuation`

This trigger should join the already covered structured result-card triggers:

- `scan missing evidence`
- `draft dissent response`
- `check upgrade conditions`
- `prepare progress summary`

---

## Why this closure is lawful

`suggest canonical continuation` naturally supports a bounded `continuation_suggestion` result card.
It can remain visibly draft-only while still giving a richer cockpit-native next-step surface.

This closes the current coverage gap without increasing authority.

---

## Current non-scope

COCKPIT2-B-C is not a runtime move, not a mutation move, and not a team-layer move.
Its only job is to complete structured draft result-card coverage for the current trigger set.
