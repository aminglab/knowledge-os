# COCKPIT4-A-B Entry Decision v1

## Status

Entered at current threshold.

## Function

This file records the entry decision for **COCKPIT4-A-B / Public Candidate Index and Registry Floor v1**.

COCKPIT4-A-A emitted the first bounded `public_candidate_object` sample.
The next lawful move is not to multiply samples immediately.
It is to create a minimal index surface so candidate objects can be discovered, counted, and checked without being mistaken for published public-layer objects.

---

## One-sentence entry verdict

Current entry verdict:

> **ENTER_COCKPIT4_A_B_PUBLIC_CANDIDATE_INDEX_FLOOR**
>
> The project may now open COCKPIT4-A-B because one concrete public-candidate sample exists, and the next lawful threshold is to register candidate metadata in a compact, auditable index while preserving non-publication and non-finalization discipline.

---

## Immediate purpose

COCKPIT4-A-B exists to do four things:

1. define a minimal public-candidate registry surface;
2. index emitted candidate objects without converting them into publications;
3. expose candidate count, candidate id, source kind, status, and snapshot boundary references;
4. attach checker-visible consistency between registry entries and candidate JSON files.

---

## Boundary

COCKPIT4-A-B does **not**:

- authorize publication
- authorize verdict finalization
- authorize governed mutation
- claim public-truth status
- create a public release catalogue
- require more candidate samples

It only introduces a registry floor for already emitted public-candidate samples.
