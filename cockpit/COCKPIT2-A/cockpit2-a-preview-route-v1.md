# COCKPIT2-A Preview Route v1

## Status

Drafted for current threshold.

## Function

This file records the minimum local preview route for the COCKPIT2-A interactive prototype.
It is intentionally smaller than a repository front-door exposure route.
It only gives the bounded interactive lift one lawful local preview path.

---

## Local preview helper

The current local helper is:

- `cockpit/COCKPIT2-A/prototype/preview_cockpit2_a.py`

The helper exists only to preview the bounded interactive prototype locally.
It does not claim live runtime behavior, write-capable cockpit behavior, or production frontend status.

---

## Preview command

From `cockpit/COCKPIT2-A/prototype/` run:

```bash
python preview_cockpit2_a.py
```

---

## Companion smoke route

The companion smoke route is recorded in:

- `cockpit/COCKPIT2-A/cockpit2-a-preview-smoke-checklist-v1.md`

The preview route and the smoke route must stay aligned.

---

## Route honesty

The right reading is:

> local preview of a bounded interactive cockpit lift.

The wrong reading is:

> live governed application preview.
