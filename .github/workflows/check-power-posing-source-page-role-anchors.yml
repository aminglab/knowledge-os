name: Check power-posing source page role anchors

on:
  push:
    paths:
      - "pilots/living-knowledge-case/cases/power-posing/sources/**"
      - "pilots/living-knowledge-case/cases/power-posing/source-page-role-anchors-v1.md"
      - "scripts/check_power_posing_source_page_role_anchors.py"
      - ".github/workflows/check-power-posing-source-page-role-anchors.yml"
  pull_request:
    paths:
      - "pilots/living-knowledge-case/cases/power-posing/sources/**"
      - "pilots/living-knowledge-case/cases/power-posing/source-page-role-anchors-v1.md"
      - "scripts/check_power_posing_source_page_role_anchors.py"
      - ".github/workflows/check-power-posing-source-page-role-anchors.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check-source-page-role-anchors:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run source page role-anchor check
        run: python scripts/check_power_posing_source_page_role_anchors.py
