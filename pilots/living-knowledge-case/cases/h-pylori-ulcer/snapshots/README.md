# Snapshots

This directory contains publication snapshots for the `h-pylori-ulcer` case.

At the current stage, the second case has only a first snapshot seed:

- [`snapshot-v0.md`](./snapshot-v0.md)

That snapshot is not yet a mature public homepage like the first case.
It is the first release-view seed that makes the new object set publicly readable.
