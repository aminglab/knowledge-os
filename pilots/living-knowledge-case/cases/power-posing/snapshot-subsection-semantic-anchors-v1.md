# Snapshot Subsection Semantic Anchors v1

This file defines the current **snapshot subsection semantic anchors v1** for the `power-posing` pilot.

Its purpose is narrow:

- keep the main semantic commitments inside `snapshot-v2.md` from drifting into vague narrative prose,
- preserve the minimal role of the most important snapshot subsections,
- and give the snapshot layer a checker-friendly semantic anchor surface.

This file is case-scoped.
It does **not** claim to be a repository-wide snapshot doctrine.

---

## Why this layer exists

The current snapshot-section layering rule already protects structure:

- `Title`
- `What this page is`
- `Current visible judgment`
- `Why this case matters`
- `Original claim neighborhood`
- `What changed later`
- `Snapshot reading path`
- `Included objects`

That is necessary.
It is not sufficient.

A snapshot can keep all those headings and still slowly lose the specific semantic commitments that make the case readable as this case rather than as generic reproducibility prose.

This file exists to stop that drift.

---

## Entry surface

Each subsection anchor entry below exposes at least:

- `Section`
- `Required anchors`

The anchors are intentionally modest.
They do not freeze every sentence of snapshot prose.
They only preserve the minimal phrases that should remain visible if the subsection is still honestly describing its current role.

---

## Current entries

### `Original claim neighborhood`
- Section: `Original claim neighborhood`
- Required anchors:
  - `higher testosterone`
  - `lower cortisol`
  - `greater feelings of power`
  - `more tolerance for risk`
  - `C-0001`
  - `E-0001`
  - `Carney_Cuddy_Yap_2010`
  - `Early_public_amplification_context`

### `What changed later`
- Section: `What changed later`
- Required anchors:
  - `empirical challenge`
  - `internal withdrawal of support`
  - `methodological attack`
  - `Lineage split`
  - `D-0001`
  - `D-0002`
  - `D-0003`
  - `C-0002`
  - `Ranehill_et_al_2015`
  - `Dana_Carney_2016_statement`
  - `Simmons_Simonsohn_2016`
  - `TED_Corrections_2017`

### `Why the descendant claim is a separate object`
- Section: `Why the descendant claim is a separate object`
- Required anchors:
  - `different claim`
  - `same lineage`
  - `binary switch`
  - `weaken`
  - `split`
  - `survive`

---

## Current note

These semantic anchors are deliberately light.

If the snapshot later changes wording substantially, the snapshot and this file should be updated together.
The goal is not to freeze style.
The goal is to preserve subsection identity.
