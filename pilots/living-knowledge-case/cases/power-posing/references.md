# References

This page is now a **reader-facing entrypoint** for the power-posing pilot reference layer.

The current **stable source metadata layer** lives here:

- [`references-metadata-v1.md`](./references-metadata-v1.md)

The current **public source page seed layer** now lives here:

- [`sources/README.md`](./sources/README.md)

That means the reference surface now has three adjacent roles:

- `references.md` as the thin reader-facing source entrypoint,
- `references-metadata-v1.md` as the stable metadata layer,
- and `sources/` as the first public-facing source-page layer.

---

## Reader path

If you are reading this case from the public-facing side, use this order:

1. [`snapshots/snapshot-v2.md`](./snapshots/snapshot-v2.md) — current living snapshot
2. [`references-metadata-v1.md`](./references-metadata-v1.md) — stable source metadata layer
3. [`sources/README.md`](./sources/README.md) — current public source page index
4. [`case.md`](./case.md) — case overview
5. [`timeline/events.md`](./timeline/events.md) — timeline view

---

## Public source pages

The current public source-page layer is now available here:

- [`sources/README.md`](./sources/README.md)

That index then routes to the current first source pages for:

- original claim source,
- replication challenge source,
- internal withdrawal-of-support source,
- methodological attack source,
- public amplification source,
- and public correction / reframing source.

---

## Current status

This file is intentionally thin.

It no longer tries to be all of the following at once:

- a reader-facing entry page,
- the full stable metadata layer,
- and a first-class source-page surface.

For the current canonical source metadata surface, use:

- [`references-metadata-v1.md`](./references-metadata-v1.md)

For the current public-facing source-page layer, use:

- [`sources/README.md`](./sources/README.md)

---

## Current note

This is still a transition structure in the pilot.

The source surface now has:

- a thin reader entrypoint,
- a stable metadata layer,
- and a first seed source-page layer.

A later repository-wide source doctrine may still merge some of these roles differently.
But at the current stage, this layered structure is useful because it keeps:

- entry,
- metadata,
- and public source reading

from collapsing into one overloaded file.
